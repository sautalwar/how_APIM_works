<!-- 
  GLOBAL POLICY — Applied to ALL APIs in the APIM instance
  This policy enforces baseline security across every API.
-->
<policies>
    <inbound>
        <!-- Enforce HTTPS -->
        <choose>
            <when condition="@(context.Request.OriginalUrl.Scheme != &quot;https&quot;)">
                <return-response>
                    <set-status code="403" reason="HTTPS Required" />
                    <set-header name="Content-Type" exists-action="override">
                        <value>application/json</value>
                    </set-header>
                    <set-body>{"error": "HTTPS is required for all API calls"}</set-body>
                </return-response>
            </when>
        </choose>

        <!-- Global rate limiting (per IP) -->
        <rate-limit-by-key calls="60" 
                           renewal-period="60" 
                           counter-key="@(context.Request.IpAddress)"
                           remaining-calls-header-name="X-RateLimit-Remaining"
                           total-calls-header-name="X-RateLimit-Limit" />

        <!-- Block dangerous HTTP methods globally -->
        <choose>
            <when condition="@(new[] {&quot;TRACE&quot;, &quot;CONNECT&quot;}.Contains(context.Request.Method))">
                <return-response>
                    <set-status code="405" reason="Method Not Allowed" />
                    <set-body>{"error": "HTTP method not allowed"}</set-body>
                </return-response>
            </when>
        </choose>

        <!-- Set correlation ID for tracing -->
        <set-header name="X-Correlation-Id" exists-action="skip">
            <value>@(context.RequestId.ToString())</value>
        </set-header>

        <base />
    </inbound>

    <backend>
        <base />
    </backend>

    <outbound>
        <!-- Remove server fingerprinting headers -->
        <set-header name="X-Powered-By" exists-action="delete" />
        <set-header name="X-AspNet-Version" exists-action="delete" />
        <set-header name="Server" exists-action="delete" />

        <!-- Add security headers -->
        <set-header name="X-Content-Type-Options" exists-action="override">
            <value>nosniff</value>
        </set-header>
        <set-header name="X-Frame-Options" exists-action="override">
            <value>DENY</value>
        </set-header>
        <set-header name="Strict-Transport-Security" exists-action="override">
            <value>max-age=31536000; includeSubDomains; preload</value>
        </set-header>
        <set-header name="Content-Security-Policy" exists-action="override">
            <value>default-src 'none'</value>
        </set-header>
        <set-header name="Referrer-Policy" exists-action="override">
            <value>strict-origin-when-cross-origin</value>
        </set-header>

        <!-- Add correlation ID to response -->
        <set-header name="X-Correlation-Id" exists-action="override">
            <value>@(context.RequestId.ToString())</value>
        </set-header>

        <base />
    </outbound>

    <on-error>
        <!-- Log error details (internal only) -->
        <trace source="GlobalErrorHandler" severity="error">
            <message>@($"Error: {context.LastError.Message} | Reason: {context.LastError.Reason} | Section: {context.LastError.Section}")</message>
        </trace>

        <!-- Return safe error response — no internal details -->
        <return-response>
            <set-status code="500" reason="Internal Server Error" />
            <set-header name="Content-Type" exists-action="override">
                <value>application/json</value>
            </set-header>
            <set-header name="X-Correlation-Id" exists-action="override">
                <value>@(context.RequestId.ToString())</value>
            </set-header>
            <set-body>@{
                return new JObject(
                    new JProperty("error", "An internal error occurred"),
                    new JProperty("correlationId", context.RequestId.ToString()),
                    new JProperty("timestamp", DateTime.UtcNow.ToString("o"))
                ).ToString();
            }</set-body>
        </return-response>
    </on-error>
</policies>
